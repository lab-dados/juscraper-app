# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
/wheels/
*.egg-info

# Testes (pytest)
.pytest_cache/
report.xml

# Virtual environments
.venv

# Node / web
node_modules/
web/dist/
web/.env
web/.env.local
*.tsbuildinfo

# Wrangler / Cloudflare
.wrangler/

# Playwright / scratch
.playwright-mcp/
results-*.png

# NOTE: web/public/wheels/ (juscraper wheel vendorizado) É versionado de propósito.
proxy/.dev.vars

# Árvores de classes/assuntos (eSAJ): NÃO versionadas. Ficam na release "trees"
# do repo (geradas por scripts/gen_trees.py) e são baixadas no deploy. Localmente,
# rode scripts/gen_trees.py para popular esta pasta.
web/public/trees/
