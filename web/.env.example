# URL do proxy CORS publicado (Cloudflare Worker). Veja proxy/README.md.
VITE_PROXY_URL=https://juscraper-proxy.SEU-USUARIO.workers.dev

# Analytics (Umami). Opcional: sem isto, o analytics fica desligado.
# website-id do site criado no Umami (cloud.umami.is ou instância própria).
VITE_UMAMI_WEBSITE_ID=
# Opcional: URL do script (default = nuvem oficial). Use se auto-hospedar.
# VITE_UMAMI_SRC=https://cloud.umami.is/script.js
